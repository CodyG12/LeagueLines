---
import { listDistinctSports } from "../../lib/props";
import { sportIcon } from "../../lib/sportIcons";

interface Props {
  showCategories?: boolean;
}

const { showCategories = true } = Astro.props;

const sports = showCategories ? await listDistinctSports() : [];
const activeSport = Astro.url.pathname.startsWith("/leagues/")
  ? decodeURIComponent(Astro.url.pathname.slice("/leagues/".length))
  : null;

const user = Astro.locals.user;
---

<header class="site-header">
  <nav class="site-nav">
    <div class="nav-left">
      <a href="/" class="site-brand nav-raise"><span class="site-brand-league">League</span><span class="site-brand-lines">Lines</span></a>
    </div>

    <div class="nav-right">
      {
        user ? (
          <span class="units-pill">{user.units} units</span>
        ) : (
          <>
            <a href="/login" class="btn-outline">
              Log In
            </a>
            <a href="/signup" class="btn-outline">
              Sign Up
            </a>
          </>
        )
      }
    </div>
  </nav>

  {
    showCategories && sports.length > 0 && (
      <div class="league-tabs-row">
        <div class="league-tabs">
          <a
            href="/"
            class={Astro.url.pathname === "/" ? "league-tab active" : "league-tab"}
          >
            <svg
              class="league-tab-icon"
              viewBox="0 0 24 24"
              fill="none"
              stroke="currentColor"
              stroke-width="1.6"
              stroke-linecap="round"
              stroke-linejoin="round"
              aria-hidden="true"
            >
              <rect x="3" y="3" width="7" height="7" rx="1.5" />
              <rect x="14" y="3" width="7" height="7" rx="1.5" />
              <rect x="3" y="14" width="7" height="7" rx="1.5" />
              <rect x="14" y="14" width="7" height="7" rx="1.5" />
            </svg>
            <span>All Bets</span>
          </a>
          {sports.map((sport) => (
            <a
              href={`/leagues/${encodeURIComponent(sport)}`}
              class={sport === activeSport ? "league-tab active" : "league-tab"}
            >
              <svg
                class="league-tab-icon"
                viewBox="0 0 24 24"
                fill="none"
                stroke="currentColor"
                stroke-width="1.6"
                stroke-linecap="round"
                stroke-linejoin="round"
                aria-hidden="true"
                set:html={sportIcon(sport)}
              />
              <span>{sport}</span>
            </a>
          ))}
        </div>
      </div>
    )
  }
</header>
